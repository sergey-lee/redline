import Foundation

enum Links {
    static let github = URL(string: "https://github.com/sergey-lee/redline")!
    // GitHub Sponsors (0% fee, native button). Swap for a Ko-fi/Buy Me a Coffee
    // URL if you prefer that provider.
    static let buyMeACoffee = URL(string: "https://github.com/sponsors/sergey-lee")!
}
